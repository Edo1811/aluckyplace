// Phase 8 — CC → A conversion
// Rate: 1000 CC = 1 A (one-way, irreversible, minimum 1000 CC)
// Architecture invariant: A balance is NEVER decremented outside the shop

async function convert(_userId, _amountCC) {
  // Phase 8
  throw new Error('Not implemented yet — Phase 8');
}

module.exports = { convert };
