// Phase 6 — Higher or Lower PvP
// Both players get a random int 1–100. Higher wins. Tie = redraw.

async function draw(_seed, _nonce) {
  // Phase 6 — returns { p1Number, p2Number }
  throw new Error('Not implemented yet — Phase 6');
}

module.exports = { draw };
